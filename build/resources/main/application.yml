server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/mydb
    username: user
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect